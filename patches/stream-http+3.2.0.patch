diff --git a/node_modules/stream-http/index.js b/node_modules/stream-http/index.js
index 84bfe51..157ab95 100644
--- a/node_modules/stream-http/index.js
+++ b/node_modules/stream-http/index.js
@@ -2,7 +2,28 @@ var ClientRequest = require('./lib/request')
 var response = require('./lib/response')
 var extend = require('xtend')
 var statusCodes = require('builtin-status-codes')
-var url = require('url')
+// Use React Native compatible URL polyfill
+require('react-native-url-polyfill/auto')
+var url = {
+	parse: function(urlString) {
+		try {
+			const parsed = new URL(urlString);
+			return {
+				protocol: parsed.protocol,
+				hostname: parsed.hostname,
+				port: parsed.port || (parsed.protocol === 'https:' ? '443' : '80'),
+				pathname: parsed.pathname,
+				search: parsed.search,
+				hash: parsed.hash,
+				host: parsed.host,
+				href: parsed.href,
+				auth: null
+			};
+		} catch (e) {
+			return null;
+		}
+	}
+}
 
 var http = exports
 
