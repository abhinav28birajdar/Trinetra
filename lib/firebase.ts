// Firebase initialization for React Native
import firebase from '@react-native-firebase/app';
import auth from '@react-native-firebase/auth';
import firestore from '@react-native-firebase/firestore';

// Optionally, you can add your Firebase config here if needed for manual initialization
// (usually handled automatically by @react-native-firebase)

export { auth, firebase, firestore };

